# SPI/Plugins for KMP

## TBD

* What is SPI?
* java compatibility
    * loading from functions/classes/properties/objects/etc
* compile-time vs runtime service discovery
* why SPI is needed in Java
* why SPI is needed in KMP
* how different languages do SPI like things
* what to do if there is no SPI in language
* how SPI compares to ServiceLocater pattern and DI frameworks
* eager initialization (klibs)
* naming (SPI vs plugins)
* JPMS support + jlink + graal vm
* out of the box helpers for single instance and multiple instances
* KBT ServiceLoader usage - classpath based
* what to do in tests? Overrides?
* annotation vs keyword - DI in the language?
* dlopen in native world! - openssl engines
* case with api/impl modules
* coroutines MainDispatcher
* library vs application flavour
* build tools control of loaded services?
* what could be done for runtime discovery for Kotlin/Wasm/JS/Native
